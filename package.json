{
  "name": "example-mcp-connector",
  "version": "0.1.0",
  "private": true,
  "description": "Remote MCP server + Claude Custom Connector skeleton: self-hosted OAuth 2.1 authorization server (PKCE + Dynamic Client Registration) that federates login to your identity provider. Deploys to Cloud Run.",
  "type": "module",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "start": "node dist/index.js",
    "dev": "tsx watch src/index.ts",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.12.0",
    "@google-cloud/firestore": "^7.10.0",
    "express": "^4.21.2",
    "googleapis": "^144.0.0",
    "jose": "^5.9.6",
    "pino": "^9.5.0",
    "pino-http": "^10.3.0",
    "zod": "^3.24.1"
  },
  "devDependencies": {
    "@types/express": "^4.17.21",
    "@types/node": "^22.10.0",
    "tsx": "^4.19.2",
    "typescript": "^5.7.2"
  }
}
